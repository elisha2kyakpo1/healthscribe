<div class="menu bars" data-controller="toggle">
  <div class="w-100 py-2 header d-flex">
    <button data-action="toggle#toggle keydown->toggle#closeOnEscape" class="">
      <i class="py-3 bar text-white ml-auto fa-solid fa-bars"></i>
    </button>
  </div>
    <ul class="navlist" aria-expanded="false" data-toggle-target="ariaExpander">
      <% if current_user %>
        <li><%= link_to "Hi, #{current_user.full_name}", class: "get-start" %></li>
        <li><%= link_to "Edit profile", edit_user_registration_path, class: 'nav-link' %></li>
        <li><%= link_to 'Sign out', destroy_user_session_path, data: { turbo_method: :delete }, class: "login text-dark" %></li>
        <% else %>
        <li> <%= link_to 'Sign in', user_session_path, class: "login" %></li>
      <% end %>
    <ul>
</div>